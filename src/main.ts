function nextGreatestLetter(letters: string[], target: string): string {
  let l = 0, r = letters.length - 1;
  while (l < r) {
    const m = Math.floor((l + r) / 2);
    if (letters[m].charCodeAt(0)) {
      
    }
  }
};
